{% extends 'base.html.twig' %}

{% block title %}Edit CatLivre{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mt-4 mb-4">Edit CatLivre</h1>

    <div class="card">
        <div class="card-body">
            {{ include('admin_cat_livre/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('app_admin_cat_livre_index') }}" class="btn btn-primary">Back to List</a>

            {{ include('admin_cat_livre/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
