{% extends 'base.html.twig' %}

{% block title %}CatLivre{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4 mb-4">CatLivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_livre.id }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ cat_livre.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_cat_livre_index') }}" class="btn btn-primary">Back to List</a>
    <a href="{{ path('app_admin_cat_livre_edit', {'id': cat_livre.id}) }}" class="btn btn-secondary">Edit</a>

    {{ include('admin_cat_livre/_delete_form.html.twig') }}
</div>

{% endblock %}
