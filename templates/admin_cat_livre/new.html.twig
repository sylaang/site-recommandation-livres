{% extends 'base.html.twig' %}

{% block title %}New CatLivre{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mt-4 mb-4">Create new CatLivre</h1>

    <div class="card">
        <div class="card-body">
            {{ include('admin_cat_livre/_form.html.twig') }}

            <a href="{{ path('app_admin_cat_livre_index') }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div>

{% endblock %}
